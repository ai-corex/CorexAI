const [sortBy, setSortBy] = useState("date");

const sorted = [...tokens].sort((a,b)=>{
  if(sortBy==="name") return a.name.localeCompare(b.name);
  if(sortBy==="date") return new Date(b.launch_date)-new Date(a.launch_date);
});
